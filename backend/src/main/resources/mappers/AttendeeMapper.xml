<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.group25.db.service.AttendeeMapper">
    <insert id="insertUser" parameterType="com.group25.db.model.Attendee">
        INSERT INTO attendees (first_name, last_name, user_id, password, phone, address)
        VALUES (#{firstName}, #{lastName}, #{userId}, #{password}, #{phone}, #{address})
    </insert>

    <select id="selectUserByEmail" parameterType="Long" resultType="com.group25.db.model.Attendee">
        SELECT first_name AS firstName, last_name AS lastName, user_id AS userId, password, phone, address
        FROM attendees
        WHERE user_id = #{userId}
    </select>
</mapper>